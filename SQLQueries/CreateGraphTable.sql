CREATE TABLE graph (
                       start_node_id INTEGER,
                       end_node_id   INTEGER,
                       dist          INTEGER,
                       way_id        INTEGER
);
